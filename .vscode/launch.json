{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C program",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "cwd": "${fileDirname}",
      "preLaunchTask": "build C (clang)"
    }
  ]
}

